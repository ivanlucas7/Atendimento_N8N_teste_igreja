# Arquivos com segredos reais — nunca commitar
.env

# Configuração dos scripts gerada a partir do .env — NUNCA commitar
**/config.js

# Documentação e schema — apenas para uso local
*.md
01_database_schema.sql
